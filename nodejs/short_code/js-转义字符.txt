JS中的转义字符是反斜杠（\）
常用的转义字符有：\’、\"、\、\n、\r、\t 等。

在 JS 代码中 成对的单引号 不能多对同时使用，双引号也是；如果同时使用多对单引号或双引号就会报错，提示有意外的字符

对反斜杠进行转义：

// 美滴很啊，美滴很！
var str = '显示\\反\\斜杠';
console.log(str)


\n	换行符，遇到该转义符会进行换行
\t	tab 缩进
\b	空格